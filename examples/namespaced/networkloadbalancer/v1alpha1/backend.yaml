apiVersion: networkloadbalancer.oci.m.upbound.io/v1alpha1
kind: Backend
metadata:
  labels:
    testing.upbound.io/example-name: backend_label_1
  name: backend1
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    networkLoadBalancerIdSelector:
      matchLabels:
        testing.upbound.io/example-name: networkloadbalancer_label_1
    backendSetName: "backendset1"
    ipAddress: "10.0.1.3"
    port: 10
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
