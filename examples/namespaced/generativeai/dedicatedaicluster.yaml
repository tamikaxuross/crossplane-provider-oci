apiVersion: generativeai.oci.m.upbound.io/v1alpha1
kind: DedicatedAiCluster
metadata:
  annotations:
    meta.upbound.io/example-id: generativeai/v1alpha1/dedicatedaicluster
  labels:
    testing.upbound.io/example-name: test_hosting_cluster
  name: test-hosting-cluster
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    compartmentIdSelector:
      matchLabels:
        testing.upbound.io/example-name: example
    description: ${var.hosting_cluster_description}
    displayName: ${var.hosting_cluster_display_name}
    freeformTags: ${var.test_freeform_tags}
    type: HOSTING
    unitCount: ${var.hosting_cluster_unit_count}
    unitShape: ${var.hosting_cluster_shape}
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
