apiVersion: functions.oci.m.upbound.io/v1alpha1
kind: InvokeFunction
metadata:
  labels:
    testing.upbound.io/example-name: invokefunction_label_1
  name: invokefunction1
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    functionIdSelector:
      matchLabels:
        testing.upbound.io/example-name: function_label_1
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
