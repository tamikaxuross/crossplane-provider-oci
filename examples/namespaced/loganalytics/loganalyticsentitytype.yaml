apiVersion: loganalytics.oci.m.upbound.io/v1alpha1
kind: LogAnalyticsEntityType
metadata:
  annotations:
    meta.upbound.io/example-id: loganalytics/v1alpha1/loganalyticsentitytype
  labels:
    testing.upbound.io/example-name: test_log_analytics_entity_type_required
  name: test-log-analytics-entity-type-required
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    name: tf-example-entity-type-req
    namespace: ${data.oci_objectstorage_namespace.ns.namespace}
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
