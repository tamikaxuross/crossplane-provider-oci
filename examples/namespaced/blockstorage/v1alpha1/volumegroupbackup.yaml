apiVersion: blockstorage.oci.m.upbound.io/v1alpha1
kind: VolumeGroupBackup
metadata:
  labels:
    testing.upbound.io/example-name: volumegroupbackup_label_1
  name: volumegroupbackup1
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    # Refer to following terraform example for brief resource fields
    # https://registry.terraform.io/providers/oracle/oci/7.27.0/docs/resources/core_volume_group_backup
    volumeGroupIdSelector:
      matchLabels:
        testing.upbound.io/example-name: volumegroup_label_1
    displayName: volumegroupbackup1
    type: FULL
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
