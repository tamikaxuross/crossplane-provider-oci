apiVersion: aidataplatform.oci.m.upbound.io/v1alpha1
kind: AiDataPlatform
metadata:
  annotations:
    meta.upbound.io/example-id: aidataplatform/v1alpha1/aidataplatform
  labels:
    testing.upbound.io/example-name: test_ai_data_platform
  name: test-ai-data-platform
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    aiDataPlatformType: ${var.ai_data_platform_ai_data_platform_type}
    compartmentIdSelector:
      matchLabels:
        testing.upbound.io/example-name: example
    defaultWorkspaceName: ${var.ai_data_platform_workspace_name}
    displayName: ${var.ai_data_platform_display_name}
    freeformTags: ${var.ai_data_platform_freeform_tags}
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
