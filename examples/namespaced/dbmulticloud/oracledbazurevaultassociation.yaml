apiVersion: dbmulticloud.oci.m.upbound.io/v1alpha1
kind: OracleDbAzureVaultAssociation
metadata:
  annotations:
    meta.upbound.io/example-id: dbmulticloud/v1alpha1/oracledbazurevaultassociation
  labels:
    testing.upbound.io/example-name: test_oracle_db_azure_vault_association
  name: test-oracle-db-azure-vault-association
  namespace: ${TARGET_NAMESPACE}
spec:
  forProvider:
    compartmentIdSelector:
      matchLabels:
        testing.upbound.io/example-name: example
    displayName: TestDbAzureVaultAssociation
    oracleDbAzureConnectorId: ${var.oracle_db_azure_connector_id}
    oracleDbAzureVaultId: ${var.oracle_db_azure_vault_id}
  # providerConfig defaults to "ClusterProviderConfig"; set providerConfigRef to "ProviderConfig" to use namespaced providerConfig.
  providerConfigRef:
    name: default
    kind: ProviderConfig
